package com.hemebiotech.analytics;

import java.util.List;
import java.util.Map;

public interface ISymptomCounter {

	public Map<String, Integer> processData(List<String> listNotCounted);
	/*
	 * void setI(); void setHeadCount() ; Object line = null while (line != null) {
	 * setI(getI() + 1); // increment i //System.out.println("symptom from file: " +
	 * line); if (line.equals("headache")) { setHeadCount(getHeadCount() + 1);
	 * //System.out.println("number of headaches: " + headCount); } else if
	 * (line.equals("rush")) { rashCount++; } else if (((List<String>)
	 * line).contains("pupils")) { pupilCount++; }
	 * 
	 * line = reader.readLine(); // get another symptom }
	 * 
	 * // fever = 12
	 */
	}
	

	
	
	
	



